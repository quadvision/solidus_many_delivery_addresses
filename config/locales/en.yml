# Sample localization file for English. Add more files in this directory for other locales.
# See https://github.com/svenfuchs/rails-i18n/tree/master/rails%2Flocale for starting points.

en:
  activerecord:
    errors:
      models:
        spree/line_item:
          attributes:
            quantity:
              line_item_quantity_not_equal_to_line_item_addresses_quantities: "Sum (%{line_item_addresses_quantity}) does not match the informed quantity for this product"
        spree/line_item_address:
          attributes:
            quantity:
              cannot_be_0: "The quantity must be more than zero"
